
* Explain **what your program does at a high level**
* Explain **why your logic works** (especially for loops, `ArrayList` processing, and the Monsters' attack logic)
* Mention any **challenges, assumptions,** or **design decisions** you made



From above, you can see that in the main file, what happens is that the program reads the secret message. It then splits up the words into an array, and from the array, it will loop through and extract each word to analyze and decrypt it. Later on, it goes through an algorithm, and finally returns the proper secret message. In the main file, after receiving the secret message, it has one long text. It first checks for spaces and then divides those spaces and adds them to an array. And then from there, we have a full array of all the words that I'm 100% sure are from the secret message. And then systematically goes through each word in the array and decrypts it. Then it shifts the array until we get the proper way we wanted the array to be sorted. I faced a couple of challenges such as in the testing, some tests were actually wrong. They were expecting the wrong result, so I had to fix those. Other than that,The hardest part was creating a mirror method, because it required flawless logic. 